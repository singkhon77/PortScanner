# PortScanner
This program is a Python-based TCP port scanner. It allows users to scan a target host or multiple hosts to determine which ports are open or closed. It supports quick scan, full scan, and custom port scan modes, with optional service detection and logging.
